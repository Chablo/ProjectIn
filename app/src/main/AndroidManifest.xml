<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.miren.projectin">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Inscription1"
            android:label="@string/title_activity_inscription1"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".Inscription_dev"
            android:label="@string/title_activity_inscription_dev"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Inscription_leader" />
        <activity
            android:name=".leader_home"
            android:label="@string/title_activity_leader_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".dev_home"
            android:label="@string/title_activity_dev_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ajouter_projet"
            android:label="@string/title_activity_ajouter_projet"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>